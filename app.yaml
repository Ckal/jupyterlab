name: jupyterlab
type: gradio-space
sdk: docker

metadata:
  space_id: Chris4K/jupyterlab
  original_url: https://huggingface.co/spaces/Chris4K/jupyterlab

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
